{{#each found}}
<button type="button" class="btn btn-primary btn-lg selectArticle" data-toggle="modal" data-target="#myModal">
{{!--<a class="goTo" href="/{{_id}}">--}}
<div class="newsItems" data-id="{{_id}}">
   <h1 class="title">Headline: {{title}}</h1>
    <p class="summary">Summary: {{summary}}</p>
    <p class="link"> Link: {{link}}</p>
</div>
{{!--</a>--}}
</button>
{{/each}}

